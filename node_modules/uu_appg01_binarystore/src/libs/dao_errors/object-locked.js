"use strict";

const BinaryStoreError = require("./binarystore-error.js");

class ObjectLocked extends BinaryStoreError {
  constructor(cause, message) {
    super(message || "UuBinary is locked.", cause);
    this.code = `${BinaryStoreError.ERROR_PREFIX}objectLocked`;
  }
}

module.exports = ObjectLocked;
