const { Lock } = require("uu_appg01_datastore");
const ExpiredLock = require("./dao_errors/expired-lock");

class BinaryStoreLock extends Lock {
  constructor(lockCode, duration = 60) {
    super(lockCode, duration);
    this.__ExpiredLock = ExpiredLock;
  }
}

module.exports = BinaryStoreLock;
